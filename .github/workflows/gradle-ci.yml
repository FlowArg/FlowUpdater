name: Gradle CI

on:
  push:
    branches: [ master ]

jobs:
  testjava:
    strategy:
      matrix:
        java: [8, 17]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK {{ matrix.java }}
        uses: actions/setup-java@v2
        with:
          java-version: {{ matrix.java }}
          distribution: 'zulu'

      - name: Build and test project with Java {{ matrix.java }}
        run: gradle test javadoc
